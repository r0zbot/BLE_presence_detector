// #include <BLEAddress.h>
// #include <string.h>


// class AddressList{
//     public:
//         bool add_address(BLEAddress search_addr){
//             for(int i = 0; i<addr_len; i++){
//                 BLEAddress addr =  BLEAddress(addr_storage[i]);
//                 if (addr.equals(search_addr)){
//                     return true; // Address already exists
//                 }
//             }
//             memcpy(addr_storage[addr_len++], search_addr.getNative(), ESP_BD_ADDR_LEN);
//             return false;
//         }

//         String toString(){
//             String out;
//             for(int i = 0; i<addr_len; i++){

//             }
//         }

//         size_t size(){
//             return addr_len;
//         }
//     private:
//         size_t addr_len = 0;
//         esp_bd_addr_t addr_storage[100];
// };